source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'
use_frameworks!

target 'TableViewConfigurator_Example' do
  pod 'TableViewConfigurator', :path => '../'
  
  target 'TableViewConfigurator_Tests' do
      inherit! :search_paths
      pod 'Quick', '~> 2.1'
      pod 'Nimble', '~> 8.0'
  end
end
